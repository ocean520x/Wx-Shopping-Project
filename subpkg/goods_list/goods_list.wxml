<!--subpkg/goods_list/goods_list.wxml-->
<view>
    <view class="goods-list">
        <view wx:for="{{goodsList}}" wx:key="*this" data-item="{{item}}" bind:tap="goToDetail">
            <my-goods goods="{{item}}" />
        </view>
    </view>
</view>